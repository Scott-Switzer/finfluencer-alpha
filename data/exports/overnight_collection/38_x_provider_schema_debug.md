# X provider dataset schema debug

Generated by `scripts/debug_x_provider_dataset_schema.py` (no full tweet bodies, no secrets).

## Offline stub

Examples:

```bash
# Fixture-only (no token)
PYTHONPATH=src .venv/bin/python scripts/debug_x_provider_dataset_schema.py \
  --provider apidojo_v2 \
  --fixture path/to/redacted_items.json

# Replay an existing run (RunPod; token in .env)
PYTHONPATH=src .venv/bin/python scripts/debug_x_provider_dataset_schema.py \
  --provider xquik \
  --run-id YOUR_RUN_ID \
  --expected-ticker TSLA \
  --window-start 2021-01-01 \
  --window-end 2021-01-08
```

Output tables include **text length** and a **12-character prefix** only.
