# X Apify actor input schema audit

This file is generated by `scripts/discover_x_apify_actor_inputs.py` (Apify **GET /v2/acts/{actorId}** only; no Actor runs).

## Offline stub

Run on **RunPod** (with `.env` token, never committed):

```bash
cd /workspace/FIN496CAPSTONE
PYTHONPATH=src .venv/bin/python scripts/discover_x_apify_actor_inputs.py
```

The script writes per-actor sections with:

- `inputSchema.properties` field names (when Apify exposes JSON Schema)
- A **proposed canary input** from `build_x_actor_input(..., source_type="advanced_search", ...)`
- Whether the probe query uses **advanced** operators (`from:`, `since:`, `$`)

No tokens, no raw tweet bodies.
